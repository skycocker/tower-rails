default: &default
  adapter: postgresql
  encoding: unicode

  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: tower_development

test:
  <<: *default
  database: tower_test

production:
  url: <%= ENV['DATABASE_URL'] %>
